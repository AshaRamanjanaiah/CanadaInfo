# CanadaInfoAppNew
This is an app which comsumes a REST service and displays photos with heading and description. 

App specifications:

Title in the toolbar is updated from json.
Displays the content as a list of rows.
Each row has right height to display its own content and no taller. 
Loads the image lazily using image caching library Glide. 
